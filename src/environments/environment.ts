export const environment = {
  production: false,
  demoMode: true,

  baseURL: "/api",
  apiBaseUrl: "/api",
  actuatorURL: "/actuator",
  auditLogPath: "/audit-logs",
  enableApiTrafficLog: false,
};
