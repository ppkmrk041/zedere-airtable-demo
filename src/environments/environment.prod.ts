export const environment = {
  production: true,
  demoMode: true,

  baseURL: "/api",
  apiBaseUrl: "/api",
  actuatorURL: "/actuator",
  auditLogPath: "/audit-logs",
  enableApiTrafficLog: false,
};
